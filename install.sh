#!/bin/bash

# sudo apt-get update && sudo apt-get install -y iperf screen

# The full update seems to be causing a timeout in openbaton. Disable it. For now at least For now at least
sudo apt-get install -y iperf screen
